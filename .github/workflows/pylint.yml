name: Lint and Test

on: [push]

jobs:
  lint_and_test:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: src
      BUCKET_NAME: test-bucket
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v2
      - name: Install uv
        uses: astral-sh/setup-uv@v5
      - run: cd apps/image_api
      - name: Lint
        run: uv run ruff check
      - name: Run tests
        run: uv run pytest tests
